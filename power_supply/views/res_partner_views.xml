<?xml version='1.0' encoding='UTF-8'?>
<odoo>

    <record id="power_supply_partner_inh_form" model="ir.ui.view">
        <field name="name">power.supply.partner.inh.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <field name="ref" position="after">
                <field name="is_dealer"/>
                <field name="is_marketeer"/>
            </field>
        </field>
    </record>

    <record id="power_supply_partner_inh_search" model="ir.ui.view">
        <field name="name">power.supply.partner.inh.search</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_res_partner_filter"/>
        <field name="arch" type="xml">
            <filter name="type_company" position="after">
                <separator/>
                <filter name="is_dealer" string="Is dealer" domain="[('is_dealer','=',True)]"/>
                <filter name="is_marketeer" string="Is marketeer" domain="[('is_marketeer','=',True)]"/>
                <filter string="Dealers and marketeers" name="dealer_marketeer"
                        domain="['|',('is_dealer','=',True),('is_marketeer','=',True)]"/>
            </filter>
        </field>
    </record>

</odoo>