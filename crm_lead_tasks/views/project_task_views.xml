<?xml version='1.0' encoding='UTF-8'?>
<odoo>
<!--
    <record id="scrap_task_inh_form_view" model="ir.ui.view">
        <field name="name">SCRAP Task Form View</field>
        <field name="model">project.task</field>
        <field name="type">form</field>

        <field name="inherit_id" ref="product.product_template_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='general_information']" position="after">
                <page name="opportunities" string="Opportunities" attrs="{'invisible':[('id','=',False)]}">
                    <h3>Opportunities:</h3>
                    <field name="lead_ids" domain="[('probability','!=',100)]">
                        <tree>
                            <field name="self"/>
                            <field name="user_id"/>
                            <field name="stage_id"/>
                        </tree>
                    </field>
                </page>
            </xpath>
        </field>
    </record>

-->

</odoo>
